apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: rbac
  namespace: openshift-gitops
spec:
  generators:
  - list:
      elements:
      - name: infra
        cluster: in-cluster
      - name: test
        cluster: test
  template:
    metadata:
      name: "{{ name }}-rbac"
    spec:
      project: "{{ name }}"
      source:
        repoURL: "https://github.com/robotobibok/acm-argo-multicluster"
        targetRevision: main
        path: manifests/rbac/overlays/{{ name }}
      destination:
        name: "{{ cluster }}"
      syncPolicy:
        automated:
          prune: false
          selfHeal: true